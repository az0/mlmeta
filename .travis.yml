language: r

sudo: required

env:
   matrix:
     - NOT_CRAN="true"
     - NOT_CRAN="false"

after_failure:
  - ./travis-tool.sh dump_logs

notifications:
  email:
    on_success: change
    on_failure: change
